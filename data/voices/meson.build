application_id = 'im.bernard.Paroligilo'

de1_dir = 'mbrola' / 'de1'
install_data(
  de1_dir / 'de1',
  install_dir: get_option('datadir') / 'voices' / de1_dir
)

it1_dir = 'mbrola' / 'it1'
install_data(
   it1_dir / 'it1',
   install_dir: get_option('datadir') / 'voices' / it1_dir
)
